<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lsh.wms.core.dao.stock.StockDeltaDao" >

	<resultMap id="stockDeltaResultMap" type="StockDelta">
		<id property="id" column="id"/>
		<result property="itemId" column="item_id"/>
		<result property="inhouseQty" column="inhouse_qty"/>
		<result property="allocQty" column="alloc_qty"/>
		<result property="businessId" column="business_id"/>
		<result property="type" column="type"/>
		<result property="createdAt" column="created_at"/>
		<result property="updatedAt" column="updated_at"/>
	</resultMap>
  
	<insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="StockDelta">
		insert into stock_delta (
			item_id,inhouse_qty,alloc_qty,business_id,type,created_at,
			updated_at
    	) values (
			#{itemId},#{inhouseQty},#{allocQty},#{businessId},#{type},#{createdAt},
			#{updatedAt}
    	)
	</insert>

	<update id="update" parameterType="StockDelta">
		update stock_delta
    	<set>
  			<if test="itemId != null">item_id=#{itemId},</if>
  			<if test="inhouseQty != null">inhouse_qty=#{inhouseQty},</if>
  			<if test="allocQty != null">alloc_qty=#{allocQty},</if>
  			<if test="businessId != null">business_id=#{businessId},</if>
  			<if test="type != null">type=#{type},</if>
  			<if test="createdAt != null">created_at=#{createdAt},</if>
  			<if test="updatedAt != null">updated_at=#{updatedAt}</if>
		</set>
		where id=#{id}
	</update>
  
	<select id="getStockDeltaById" parameterType="int" resultMap="stockDeltaResultMap">
		select * from stock_delta
		where ID=#{id} 
	</select>
  
	<select id="countStockDelta" parameterType="map" resultType="int">
		select count(*) from stock_delta
		<include refid="queryCondition"/>
	</select>
  
	<select id="getStockDeltaList" parameterType="map" resultMap="stockDeltaResultMap">
		select * from stock_delta
		<include refid="queryCondition"/>
		<if test="start != null and limit != null">
		limit #{start},#{limit}
		</if>
	</select>
  
	<sql id="queryCondition">
		<where>
			<if test="id != null">id=#{id}</if>
			<if test="itemId != null">and item_id=#{itemId}</if>
			<if test="inhouseQty != null">and inhouse_qty=#{inhouseQty}</if>
			<if test="allocQty != null">and alloc_qty=#{allocQty}</if>
			<if test="businessId != null">and business_id=#{businessId}</if>
			<if test="type != null">and type=#{type}</if>
			<if test="createdAt != null">and created_at=#{createdAt}</if>
			<if test="updatedAt != null">and updated_at=#{updatedAt}</if>
		</where>
	</sql>
   
</mapper>